server:
  port: 9000

spring:
  application:
    name: passjava-content

  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848


  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.56.10:3306/passjava_cms?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Shanghai
    username: root
    password: vagrant

mybatis-plus:
  mapper-locations: classpath:/mapper/**/*.xml
  global-config:
    db-config:
      id-type: auto
